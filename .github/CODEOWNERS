# Every change to this repo requires Zak's review before it can merge to `main`.
# Works together with branch protection (see ops/branch-protection.md): a support
# session or a teammate can OPEN a pull request, but it can't merge without a
# review from a code owner and passing CI.
*       @zakstowaway
