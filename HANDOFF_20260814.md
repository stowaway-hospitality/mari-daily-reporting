# Start here — cost book, 2026-08-14

Flags **85 → 65**. Two sessions' work; the second one is below the fold.

## Read this first: main was RED before any of it

Verified on a pristine clone, three tests failing, none of them mine:

    test_the_real_cost_book_holds_each_invoice_line_once
    test_no_suspect_names_reach_the_picker
    test_par_model::test_live_coverage_gate_currently_passes

The first two were **one defect with a long tail**. The FFT parser fix (42092cf)
stopped the SKU cell swallowing the UNIT word and the backfill (fdcb73f) repaired
the CODE across 41 stored invoices — but not the DESCRIPTION, which still held the
bled cell while the product name sat stranded one column left:

    code "BRL"   description "BRL Box"   raw_uom "Broccolini"

and `cogs_list.csv` **merges rather than replaces**, so 359 rows keyed by the dead
`"BRL Box"` spelling stayed alongside the clean-coded rows for the same invoice
lines. `canonical_purchasable()` maps one onto the other, so every one of those
lines was counted twice — and the chef's picker was offering "Brl" and "Cele" as
product names.

`scripts/repair_fft_swallowed_codes.py` does it in three phases, each with its own
refusal condition and every invoice total asserted unchanged:

1. **56 lines / 31 invoices** — `description ← raw_uom`, `raw_uom ← the tail`.
2. **359 orphan rows purged**, but only where the bled code appears in *no* stored
   invoice **and** a clean-code row exists on the *same* invoice. 359/359 met both;
   0 bled codes were still live. Anything failing either test is kept and named.
3. **53 stale labels refreshed.** `build_cogs_list` re-derives money and nothing
   else, deliberately — so a name written before the parser was fixed sits there
   forever. A label is not money.

The third failure was Harry Gatos' **Coconut Margarita**: built in Produce, costed
at $3.5005, and simply absent from the par model's own recipe file, which is a
separate source from the COGS pipeline. Copied across, not invented — and the two
things I would not guess at are written above it in the yaml (it has no lime line
where Stowaway's carries 30 ml, and it pours plain Rooster Blanco rather than the
coconut-washed prep that exists).

## The rule that did most of the work

> **Ratio ~1 gets a label fix. Ratio ~0.001 gets a weighing. Anything in between
> is a real question and stays open.**

Take a recipe line's quantity, multiply by the ingredient's own per-pack rate, and
compare with what the book actually charges:

    Broccolini  0.22  x $3.17/bunch = $0.6974   book $0.6945   ratio 0.996
    Lettuce     0.083 x $2.75/ea    = $0.2283   book $0.2283   ratio 1.000
    Shallot     900   x $3.30/bunch = $2970     book $10.16    ratio 0.0034

Ratio 1.0 means the number is **already being read as packs** and only the word is
wrong — so `data/recipe_line_unit_fixes.yaml` gets an entry with **no
`cost_factor`**, which the file always supported and which had never been used.
Ratio 0.001 means the line genuinely is in grams and needs a bunch weight off a set
of scales. Relabelling those would turn 900 g of shallots into 900 BUNCHES.

## Closed — 20 flags, and 18 of them move no money

* **9 label-only line-unit fixes** (broccolini, coriander, parsley, thyme, lettuce,
  lime, radish, mint, pears).
* **8 "per can" pack pins.** "can" is Lightspeed's *default*, not a container. Each
  flag itself said the price was already sane for the real unit, so `pack_qty: 1`
  divides by one and nothing moves; the two kg lines convert into grams, which is
  the conversion the recipes needed anyway.
* **2 bad seeds** — Gulli carton-as-each: garlic bread $59.81 (carton of 40) and
  pizza inserts $11.055 (box of 100), corrected to $1.4952 and $0.11055.
* **1 real 20x over-cost.** FFT `OBL20BG` states `"20kg bag"` in `raw_uom` and was
  booked at **$22.00 a kilo**. $22/20 kg = $1.10/kg against the same supplier's
  loose onion at $1.32/kg on the same run — a bag slightly cheaper per kilo than
  loose, which is what a bag is for.

## Two things I shipped and took back out

**Garlic Bread's label fix** is correct on its own merits and dropped
`Garlic Bread [Deal]` from $1.4952 to **$0.0374** — exactly 40x, flattering. The
Deal references the line by its pack-sized name, `Garlic Bread [9" x40]`; while the
recipe's line read "ml" it fell through to the single-piece product and cost
correctly *by accident*. Relabelling made the recipe a valid subrecipe, the
pack-sized reference resolved to it, and the x40 in the name divided the result. A
cosmetic flag is not worth that. Left open, reasoning in the yaml.

**Pears Green** is pinned at 1 ea and **its flag still fires, deliberately.** The
price is right and the *name* is wrong: $0.65 is not a kilo of pears (FFT bill
PGKG at $3.96/kg) but it is an ordinary price for one pear (Select Fresh bill PGRE
at $0.40 each), and the book has always treated it that way — Rocket Man takes 0.25
and is charged $0.1625. What closes it is a rename to `Pears Green [ea]` in Back
Office, not another override.

## Earlier the same day

`_adjudicated_ids()` read **every** row of `product_map.csv` as a verdict on a price
conflict. It is a bridge table. Running `bridge_stale_seeds.py --apply` — 27
mechanical name matches — silently retired the Frank's Hot Sauce and Honey Pure
conflicts, two flags whose own test comment says they must stay visible until
Lightspeed is fixed. It now counts a row only where `confidence` records a
**judgement** (the Havana Club pattern) and not a **match**. Four conflicts came
back; that is why 85 → 65 and not 85 → 61.

`data/adjudicated_prices.yaml` is new: verdicts with no price to import. Bridges go
in `product_map.csv`, verdicts go here. Ten were first written *into* product_map
with blank supplier codes and three tests went red inside a minute on
`purchase_id()`'s ValueError — *"There is no natural key, so there is no
identity."* The refusal was right and the rows were in the wrong file.

Also fixed with the money moving: **Bittermen's Tiki** 700 ml → 146 ml (Paramount's
invoice states it in words; 4.79x under), **Gyoza** 8000 g → 800 g (a
`DefaultSize` extra zero; $1.16/kg for frozen gyoza does not exist), **Onion
Spanish** $24 bag recorded as $24/kg, **Choi Sum** $9.00 against four invoices at
$1.93–$2.75, **Edamame** 2.4–3.6x above every Foodlink invoice.

**Herb Chives 6-bunch was backed out.** 17 paired invoices, median exactly 6.00x —
irresistible, and wrong twice: `pack_overrides.yaml` line 185 already holds Zak's
2026-07-26 confirmation that FFT market herbs are sold *by the bunch* (a market
bunch is one bunch ~6x the size, which is why it costs 6x), and the override landed
on the wrong observation anyway, inventing $0.275 chives. **Reading `costs.csv` is
not the same as knowing which observation WINS.**

## Detectors

`check_pack_agreement`'s two passes now share **one** definition of "outlier in
time". Pass 2 already refused to report a regime change, and its docstring names
the very case pass 1 went red on — FFT coriander $15.40 → $7.70 at the end of May,
exactly 2.00x and entirely real, herbs being a market line. Two passes disagreeing
about the same rule is worse than either answer.

The mutation suite went 15/15 → 12/15 and **not one survivor was a broken guard.**
Two were the book getting *clean*: with nothing left to find, "tolerance too tight
to ever match" returns exactly what a working detector returns. Both now carry
fixtures with a negative case beside each. The third — "the invoice UOM stops
outranking the description" — survived the *entire* gate with nothing asserting the
rule; `test_resolve_pack_uom_rank.py` asserts the behaviour now, with no data
dependency.

## The gate

    python3 -m pytest -p no:warnings -q
    node scripts/test_recipe_book_flags.mjs          # 20 assertions
    node scripts/test_recipe_flags_families.mjs      # 66 assertions
    python3 scripts/arch_guard.py                    # runs the 10 node suites too
    python3 scripts/schema_guard.py
    python3 scripts/check_pack_agreement.py --strict
    python3 scripts/check_pack_as_rate.py --strict
    python3 scripts/build_site.py
    cp data/costs.csv /tmp/b && python3 modules/recipes/pipeline/build_costs.py \
      && diff -q /tmp/b data/costs.csv               # byte-identical
    python3 scripts/audit_book.py                    # SEVERE 3, exit 1 expected
    python3 scripts/mutation_test.py                 # ~25 min, REFUSES a dirty tree

Rebuild order: `build_cogs_list` → `build_costs` → `build_ingredients` →
`build_price_compare` → `convert_lightspeed_recipes` → `build_cost_book_flags`.

## Traps sprung today

* **`mutation_test.py` rewrites the working tree for ~25 minutes.** A `git commit -A`
  mid-flight committed a live mutation — `build_cogs_list.py`'s `DERIVED` tuple
  shipped as the defect the mutation exists to prove we catch. Caught only because
  a later `git status` showed the tree disagreeing with HEAD *in the direction of
  correctness*. There is now a lockfile, a dirty-tree refusal and a banner.
* **A test can pass because of a file ABSENT from your laptop.**
  `test_no_token_is_unknown_not_a_false_all_clear` was green for months;
  *repairing the PAT* turned "no token" into "ok" and the branch it is named after
  had never run.
* **A regenerator is only safe where nothing downstream was decided by hand.**
  Re-running `build_recipe_bridge.py --apply` would now bridge Herb Coriander to
  FFT's HCDRMB — the **market** bunch at $15.40 against a retail bunch at $1.30 —
  and re-point the 6" tortilla at a different B&E code than the adjudicated one.
* The `/tmp` clone's object store corrupted mid-session (1366 files vanished, 6
  objects unreadable). HEAD was pushed, so recovery was a fresh clone plus
  carrying 43 uncommitted files across. **Push early.**

## What is left: 65 flags, and what each is blocked on

| family | n | blocked on |
|---|---|---|
| `no_recipe` | 41 | a recipe written — **not aliasable** |
| `feed_defect` | 15 | see below |
| `cook_loss` | 5 | a kitchen weighing |
| `price_conflict` | 2 | Lightspeed's side, on purpose |
| `structure` | 1 | Zak: does Fish Burrito get lime? |
| `back_office` | 1 | Zak: which beer is it? |

**The 41 `no_recipe` are not aliasable and I checked properly.** No
exact-minus-" D" match exists — "Chicken Parmy" and "Fries" are not recipes either.
The nearest matches are all different dishes (Cauliflower Tacos vs Cauliflower
Burrito at 0.70, Eggplant Parmy vs Eggplant Schnitty at 0.58), which is exactly the
guessing `product_recipe_aliases.yaml` records Zak stopping:
*"PRAWN TOAST IS DONE. STOP DOING THIS. BE THOROUGH."* The rule stands — a name
goes in that file when someone who knows the menu confirms it.

**The 15 `feed_defect`**: 5 need scales (shallot, choi sum, chives, baby's breath,
edible flower — all are genuinely gram lines against a per-piece pack, where the
book correctly refuses to cost and falls back to Lightspeed's figure); 3 are
two-worlds records adjudicated in `adjudicated_prices.yaml` but awaiting a Produce
or Back Office basis change (lemon, chives, avocado); pears green needs the Back
Office rename; garlic bread is deliberately left; cauliflower and turkish bread are
**new questions my own fix surfaced** — now that the pack reads "ea", a "180 g"
line needs a head weight, which is a fair question that the "can" label was hiding;
corn baby sweet and herb coriander are each charging ~2x and need the Produce unit.

**Still open from 2026-08-09**: 26 add-on portions, five kitchen weighings (lamb,
pork, beef roast, brisket, achiote chicken), ~22 Produce label-only units.
