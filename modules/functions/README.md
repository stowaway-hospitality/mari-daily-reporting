# functions

Two feeds, at the two ends of a function.

**Before.** `data/functions_pipeline.json` — every enquiry on the monday
FUNCTIONS ENQUIRY TRACKER, whose move it is with the log line that says so, and
what is still outstanding. Exists because the screen read the booking engine's
brief table, which has always been empty, and said "Pipeline 0" while sixty
enquiries sat on the board.

**After.** `data/functions_gp.json` — what a package function actually made.
The ticket revenue on one side, the comped tab's own line items costed against
the book on the other. Exists because the package SKUs have no costed recipe,
so in the P&L a function books at 100% GP and looks free.
